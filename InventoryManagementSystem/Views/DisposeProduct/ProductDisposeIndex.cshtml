@model  IEnumerable<InventoryManagementSystem.Domain.InDisposeMessageVM>
@{
    ViewBag.Title = "Disposal Product Details ";
}
<div class="box box-primary">
    <div class="box-header with-border">
        <div class="box-title"><i class="fa fa-list"></i> Disposal Product Details </div>
        <div class="box-tools pull-right">
            <button class="btn btn-box-tool" type="button" data-widget="collapse">
                <i class="fa fa-minus"></i>
            </button>
        </div>
    </div>
    <div class="box-body tablefont">
        <table class="table table-bordered">
            <thead class="thead-dark">
                <tr>
                    <th>Request Date</th>
                    <th>Request By</th>
                    <th>Approve By</th>
                    <th>Status</th>
                    <th>Branch Name</th>
                    <th>Department Name</th>
                    <th>Dispose</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(model => item.RequestDate)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.RequestByName)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.ApproveByName)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.Status)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.BranchName)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.DepartmentName)
                        </td>
                        <td>
                            <a href="@Url.Action("ProductDispose","DisposeProduct",new { id=item.Id})" class="btn btn-xs btn-info" title="View"><i>View</i></a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

